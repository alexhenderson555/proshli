// Barrel for `@otklik/ui`. Wave 10 will add more components — keep this
// alphabetised so diffs stay clean.

export { Button, buttonVariants, type ButtonProps } from "./components/button";
export { cn } from "./lib/utils";
